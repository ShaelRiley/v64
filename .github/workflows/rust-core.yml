name: V64 Rust core golden gate

on:
  pull_request:
    paths:
      - ".github/workflows/rust-core.yml"
      - "Cargo.toml"
      - "Cargo.lock"
      - "crates/**"
      - "tests/golden/**"
      - "prototype/js/constants.mjs"
      - "prototype/js/container.mjs"
  workflow_dispatch:

jobs:
  rust-core:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Check out pull-request head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Install pinned Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install 1.85.0 --profile minimal
          rustup default 1.85.0
          rustc --version
          cargo --version

      - name: Build and test v64-core
        run: cargo test --workspace --all-targets

      - name: Test optimized release parser
        run: cargo test --workspace --all-targets --release

      - name: Record dependency lockfile
        uses: actions/upload-artifact@v4
        with:
          name: v64-rust-cargo-lock
          path: Cargo.lock
          if-no-files-found: error
          retention-days: 30
